export default function OrdersPage() {
  return (
    <section className="rounded-[2rem] border border-white/10 bg-white/5 p-8">
      <p className="text-sm uppercase tracking-[0.3em] text-emerald-300">Orders</p>
      <h2 className="mt-3 text-3xl font-semibold text-white">Order intake placeholder</h2>
      <p className="mt-3 max-w-2xl text-sm text-slate-300">
        The foundation API exposes mock order endpoints only. Amazon import and
        order normalization workflows are intentionally deferred.
      </p>
    </section>
  );
}
