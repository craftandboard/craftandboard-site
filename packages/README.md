# Future Package Destinations

This directory is still runtime-light today. These README stubs document the intended package extraction targets for the FieldMetriq merge path without changing current build behavior.

Planned domains:
- `auth`
- `costing`
- `integrations`
- `inventory`
- `labeling`
- `manufacturing`
- `scanning`
- `shared`
- `ui`

Current implementation still primarily lives under `apps/api`, `apps/web`, and `prisma`. Extraction belongs to explicit migration branches only.
